@model Category
@{
    ViewData["Title"] = Model.Name;
}

<header class="bg-dark py-5">
    <div class="container px-1 px-lg-3 my-1">
        <div class="text-center text-white">
            <h1 class="display-4 fw-bolder">@Model.Name</h1>
            <p class="lead fw-normal text-white-50 mb-0">@Model.Description</p>
        </div>
    </div>
</header>

<div class="container px-4 px-lg-5 mt-5">
    <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">
        @foreach (var item in Model.Products)
        {
            @*<div class="col mb-5">
                <div class="card h-100">
                    <!-- Product image-->
                    <a asp-action="Detail" asp-controller="Products" asp-route-id="@Model.Id">
                        <img class="card-img-top" src="/Img/@Model.Image" alt="@Model.Name">
                    </a>
                    <!-- Product details-->
                    <div class="card-body p-4">
                        <div class="text-center">
                            <!-- Product name-->
                            <h5 class="fw-bolder">@Model.Name</h5>
                            <!-- Product price-->
                        @Model.Price ₺
                        </div>
                    </div>
                    <!-- Product actions-->
                    <div class="card-footer p-4 pt-0 border-top-0 bg-transparent">
                        <div class="text-center"><a class="btn btn-outline-dark mt-auto" asp-action="Detail" asp-controller="Products" asp-route-id="@Model.Id">İncele</a></div>
                    </div>
                </div>
            </div>*@   @*Bu satırları sildik*@
            <partial name="_ProductView" model="item" />
        }
    </div>
</div>